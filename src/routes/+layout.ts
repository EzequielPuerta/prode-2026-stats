// Prerender the whole app to static files so it can be served from GitHub Pages.
export const prerender = true;

// No server runtime on GitHub Pages; everything is a static asset.
export const ssr = true;
